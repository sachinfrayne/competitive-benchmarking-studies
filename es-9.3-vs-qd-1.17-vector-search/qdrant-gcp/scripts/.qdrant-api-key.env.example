# Qdrant API key only (keeps secrets/.secrets.env aligned with elasticsearch stacks).
# Created on first `make k8s-deploy` by ensure-qdrant-api-key-in-env.sh, or copy this file to
# .qdrant-api-key.env and set QDRANT_API_KEY manually.
# QDRANT_API_KEY=
