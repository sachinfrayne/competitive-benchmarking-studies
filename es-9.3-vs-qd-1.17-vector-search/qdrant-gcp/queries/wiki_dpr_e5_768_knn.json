{
  "name": "wiki_dpr_e5_768_knn",
  "template": {
    "vector": "{{query_vector}}",
    "params": {
      "hnsw_ef": "{{num_candidates}}",
      "quantization": {
        "rescore": true,
        "oversampling": "{{rescore}}"
      }
    },
    "limit": "{{size}}",
    "with_payload": false,
    "with_vector": false
  }
}
